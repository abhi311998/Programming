#include<bits/stdc++.h>
using namespace std;
int main()
{
	short t;
	cin>>t;
	cin.ignore();
	while(t--)
	{
		int n,i;
		cin>>n;
		vector<string> str;
		string s;
		for(i=0;i<n;i++)
		{
			cin>>s;
			str.push_back(s);
		}
		for(i=0;i<n;i++)
		{
			for(j=)
		}
	}
}
